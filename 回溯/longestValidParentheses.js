// 回溯暴搜
const longestValidParentheses = function (s) {
	s = s.split('')
	const result = []
	const traceBack = temp => {
		// 结束情况 && 处理结果
		if (!s.length) {
			result.push(temp.join(''))
		} else {
		}
	}
}
