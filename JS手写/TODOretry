function retry(fn, retries, delay) {
	// 实现代码
}

// 示例用法
const fetchData = () =>
	new Promise((resolve, reject) => {
		const success = Math.random() > 0.5
		setTimeout(() => (success ? resolve('Data') : reject('Error')), 500)
	})

retry(fetchData, 3, 1000).then(console.log).catch(console.error)
